/*
import {ResultCode} from "../utils/result-code";
import {HTTP_STATUSES} from "../../../utils/utils";

export function mapServiceCodeToHttpStatus(resultCode:ResultCode) {
    switch (resultCode) {
        case 0:
            return HTTP_STATUSES.CREATED_201;
        case 20:
            return HTTP_STATUSES.BAD_REQUEST_400;
        case 21:
            return HTTP_STATUSES.BAD_REQUEST_400;
        case 22:
            return HTTP_STATUSES.BAD_REQUEST_400;

        default:
            return 500;
    }
}
*/
